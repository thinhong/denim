) +
theme_bw()
ggsave("../manuscript_plots/denim_memory_scale_timeStep.png",
width = 8, height = 5, dpi = 300)
pkgdown::build_site(lazy=TRUE)
pkgdown::build_site(lazy=TRUE)
pkgdown::build_site(lazy=TRUE)
pkgdown::build_site(lazy=TRUE)
devtools::check()
pkgdown::preview_site()
pkgdown::build_site(lazy=TRUE)
pkgdown::preview_page('reference/denim-package.html')
pkgdown::build_site(lazy=TRUE)
pkgdown::build_site(lazy=TRUE)
pkgdown::build_site(lazy=TRUE)
pkgdown::build_site(lazy=TRUE)
pkgdown::preview_site()
library(denim, lib.loc = "/Users/anhptq/Library/R/arm64/4.3/library")
remove.packages("denim", lib="~/Library/R/arm64/4.3/library")
usethis::use_version("patch")
devtools::check()
1/sqrt(20)
1/sqrt(13)
1/sqrt(2)
1/sqrt(4-)
1/sqrt(40)
devtools::submit_cran()
devtools::submit_cran()
1/sqrt(2)
1/sqrt(13)
1/sqrt(3)
1/sqrt(17)
1/sqrt(40)
sqrt(exp(0.17**2) - 1)
install.packages("denim")
install.packages("denim")
library(denim)
?sim
library(tidyverse)
# loading cached run time if available
cached_runtime <- if (file.exists("../supplements/cached_runtime.rds")) {
readRDS("../supplements/cached_runtime.rds")
} else {
NULL
}
# compute min, max, variance
# lapply(cached_runtime, function(x) c(min = min(x), max = max(x), var = var(x)))
total_runs <- 50L # number of runs
sim_duration <- 180 # duration of simulation
library(denim)
timeStep <- 0.01
errorTolerance <- 0.001
denim_model <- denim_dsl({
S -> E = (R0/tr) * S * (I/N)
E -> I = d_gamma(rate = 1/4, shape = 2)
I -> R = d_gamma(rate = 1/3, shape = 2)
})
initialValues <- c(S = 999999, E = 1, I= 0, R= 0)
parameters <- c(R0 = 3.5,
tr = 3*2, # compute mean recovery time, for gamma it's scale*shape
N = 1e6)
errorTolerances <- c(0.001, 0.005, 0.01, 0.05, 0.1)
benchmark_out <- lapply(errorTolerances,
\(x){
bench_out <- bench::mark(
sim(transitions = denim_model,
initialValues = initialValues,
parameters = parameters,
simulationDuration = sim_duration, errorTolerance = x,
timeStep = 0.01
)
)
bench_out
}
)
runtime_scaling <- sapply(benchmark_out, \(bm){
bm$median
})
memory_scaling <- sapply(benchmark_out, \(bm){
as.numeric(bm$mem_alloc)/(1024)
})
data.frame(
errorTolerances = errorTolerances,
runtime_scaling = runtime_scaling
) %>%
ggplot(aes(x = errorTolerances, y = runtime_scaling)) +
geom_point(color = "cornflowerblue", shape = 1, size=2) +
geom_line(color = "cornflowerblue") +
geom_vline(xintercept = timeStep,
color = "red", linetype = "dashed", linewidth = 0.5) +
geom_vline(xintercept = 1,
color = "blueviolet", linetype = "dashed", linewidth = 0.5) +
annotate("text",
x = 0.001,
y = max(runtime_scaling),
label = "default error tolerance",
color = "red",
hjust = 0) +
labs(
x = "Error tolerance",
y = "Median run time (in seconds)"
) +
theme_bw()
data.frame(
errorTolerances = errorTolerances,
runtime_scaling = runtime_scaling
) %>%
ggplot(aes(x = errorTolerances, y = runtime_scaling)) +
geom_point(color = "cornflowerblue", shape = 1, size=2) +
geom_line(color = "cornflowerblue") +
geom_vline(xintercept = timeStep,
color = "red", linetype = "dashed", linewidth = 0.5) +
# geom_vline(xintercept = 1,
#            color = "blueviolet", linetype = "dashed", linewidth = 0.5) +
annotate("text",
x = 0.001,
y = max(runtime_scaling),
label = "default error tolerance",
color = "red",
hjust = 0) +
labs(
x = "Error tolerance",
y = "Median run time (in seconds)"
) +
theme_bw()
data.frame(
errorTolerances = errorTolerances,
runtime_scaling = runtime_scaling
) %>%
ggplot(aes(x = errorTolerances, y = runtime_scaling)) +
geom_point(color = "cornflowerblue", shape = 1, size=2) +
geom_line(color = "cornflowerblue") +
geom_vline(xintercept = 0.001,
color = "red", linetype = "dashed", linewidth = 0.5) +
# geom_vline(xintercept = 1,
#            color = "blueviolet", linetype = "dashed", linewidth = 0.5) +
annotate("text",
x = 0.001,
y = max(runtime_scaling),
label = "default error tolerance",
color = "red",
hjust = 0) +
labs(
x = "Error tolerance",
y = "Median run time (in seconds)"
) +
theme_bw()
runtime_scaling <- sapply(benchmark_out, \(bm){
bm$median
})
runtime_scaling
errorTolerances <- c(0.001, 0.005, 0.01, 0.05, 0.1)
benchmark_out <- lapply(errorTolerances,
\(x){
bench_out <- bench::mark(
sim(transitions = denim_model,
initialValues = initialValues,
parameters = parameters,
simulationDuration = sim_duration, errorTolerance = x,
timeStep = 0.01
)
)
bench_out
}
)
runtime_scaling <- sapply(benchmark_out, \(bm){
bm$median
})
memory_scaling <- sapply(benchmark_out, \(bm){
as.numeric(bm$mem_alloc)/(1024)
})
runtime_scaling
data.frame(
errorTolerances = errorTolerances,
runtime_scaling = runtime_scaling
) %>%
ggplot(aes(x = errorTolerances, y = runtime_scaling)) +
geom_point(color = "cornflowerblue", shape = 1, size=2) +
geom_line(color = "cornflowerblue") +
geom_vline(xintercept = 0.001,
color = "red", linetype = "dashed", linewidth = 0.5) +
# geom_vline(xintercept = 1,
#            color = "blueviolet", linetype = "dashed", linewidth = 0.5) +
annotate("text",
x = 0.001,
y = max(runtime_scaling),
label = "default error tolerance",
color = "red",
hjust = 0) +
labs(
x = "Error tolerance",
y = "Median run time (in seconds)"
) +
theme_bw()
errorTolerances <- c(0.001, 0.005, 0.01, 0.025, 0.05, 0.1)
benchmark_out <- lapply(errorTolerances,
\(x){
bench_out <- bench::mark(
sim(transitions = denim_model,
initialValues = initialValues,
parameters = parameters,
simulationDuration = sim_duration, errorTolerance = x,
timeStep = 0.01
)
)
bench_out
}
)
runtime_scaling <- sapply(benchmark_out, \(bm){
bm$median
})
memory_scaling <- sapply(benchmark_out, \(bm){
as.numeric(bm$mem_alloc)/(1024)
})
data.frame(
errorTolerances = errorTolerances,
runtime_scaling = runtime_scaling
) %>%
ggplot(aes(x = errorTolerances, y = runtime_scaling)) +
geom_point(color = "cornflowerblue", shape = 1, size=2) +
geom_line(color = "cornflowerblue") +
geom_vline(xintercept = 0.001,
color = "red", linetype = "dashed", linewidth = 0.5) +
# geom_vline(xintercept = 1,
#            color = "blueviolet", linetype = "dashed", linewidth = 0.5) +
annotate("text",
x = 0.001,
y = max(runtime_scaling),
label = "default error tolerance",
color = "red",
hjust = 0) +
labs(
x = "Error tolerance",
y = "Median run time (in seconds)"
) +
theme_bw()
data.frame(
errorTolerances = errorTolerances,
memory_scaling = memory_scaling
) %>%
ggplot(aes(x = errorTolerances, y = memory_scaling)) +
geom_point(color = "cornflowerblue", shape = 1, size=2) +
geom_line(color = "cornflowerblue") +
geom_vline(xintercept = 0.001,
color = "red", linetype = "dashed", linewidth = 0.5) +
annotate("text",
x = 0.001,
y = max(memory_scaling),
label = "default error tolerance",
color = "red",
hjust = 0) +
labs(
x = "Error tolerance",
y = "Memory allocated (in KBs)",
caption = "*Memory allocations are tracked by Rprofmem, which records only allocations made through R’s memory allocator"
) +
theme_bw()
errorTolerances <- c(0.001, 0.005, 0.01, 0.025, 0.05, 0.1)
benchmark_out <- lapply(errorTolerances,
\(x){
bench_out <- bench::mark(
sim(transitions = denim_model,
initialValues = initialValues,
parameters = parameters,
simulationDuration = sim_duration, errorTolerance = x,
timeStep = 0.01
)
)
bench_out
}
)
runtime_scaling <- sapply(benchmark_out, \(bm){
bm$median
})
memory_scaling <- sapply(benchmark_out, \(bm){
as.numeric(bm$mem_alloc)/(1024)
})
data.frame(
errorTolerances = errorTolerances,
memory_scaling = memory_scaling
) %>%
ggplot(aes(x = errorTolerances, y = memory_scaling)) +
geom_point(color = "cornflowerblue", shape = 1, size=2) +
geom_line(color = "cornflowerblue") +
geom_vline(xintercept = 0.001,
color = "red", linetype = "dashed", linewidth = 0.5) +
annotate("text",
x = 0.001,
y = max(memory_scaling),
label = "default error tolerance",
color = "red",
hjust = 0) +
labs(
x = "Error tolerance",
y = "Memory allocated (in KBs)",
caption = "*Memory allocations are tracked by Rprofmem, which records only allocations made through R’s memory allocator"
) +
theme_bw()
library(deSolve)
parameters <- c(gamma_rate_I = 1/4, shape_I=2,
gamma_rate_R = 1/3, shape_R = 2,
R0 = 3.5, N = 1e6)
initialValues <- c(S = 999999, E1 = 1,
E2 = 0, E = 0, I1=0,
I2=0, I=0, R=0
)
# --- Transition def for deSolve
transition_func <- function(t, state, param){
with(as.list( c(state, param) ), {
tr = shape_R*(1/gamma_rate_R)
dS = - (R0/tr) * S * I/N
# apply linear chain trick
dE1 = (R0/tr) * S * I/N - gamma_rate_I*E1
dE2 = gamma_rate_I*E1 - gamma_rate_I*E2
dE = dE1 + dE2
dI1 = gamma_rate_I*E2 - gamma_rate_R*I1
dI2 = gamma_rate_R*I1 - gamma_rate_R*I2
dI =  dI1 + dI2
dR = gamma_rate_R*I2
list(c(dS, dE1, dE2, dE, dI1, dI2, dI, dR))
})
}
times <- seq(0, sim_duration, 1)
ode_mod <- ode(y = initialValues, times = times, parms = parameters, func = transition_func)
ode_mod <- as.data.frame(ode_mod)
# compute prop for deSolve output as well
rescaled_desolve <- ode_mod
rescaled_desolve[, c("S","E", "I", "R")] <- rescaled_desolve[, c("S","E", "I", "R")]/1e6
errorTolerances <- c(0.001, 0.01, 0.1, 0.2)
# --- generate output at each time step -----
out <- map_dfr(errorTolerances, \(x){
data <- sim(
transitions = denim_model,
initialValues = initialValues,
parameters = parameters,
simulationDuration = sim_duration,
timeStep = 0.01,
errorTolerance = x
) %>%
mutate(
S = S/1e6, E = E/1e6, I = I/1e6, R = R/1e6
)
data %>%
filter(Time %% 1 == 0) %>%
mutate(
approach = paste0("denim dt=", x)
)
})
parameters
initialValues <- c(S = 999999, E = 1, I= 0, R= 0)
parameters <- c(R0 = 3.5,
tr = 3*2, # compute mean recovery time, for gamma it's scale*shape
N = 1e6)
errorTolerances <- c(0.001, 0.01, 0.1, 0.2)
# --- generate output at each time step -----
out <- map_dfr(errorTolerances, \(x){
data <- sim(
transitions = denim_model,
initialValues = initialValues,
parameters = parameters,
simulationDuration = sim_duration,
timeStep = 0.01,
errorTolerance = x
) %>%
mutate(
S = S/1e6, E = E/1e6, I = I/1e6, R = R/1e6
)
data %>%
filter(Time %% 1 == 0) %>%
mutate(
approach = paste0("denim dt=", x)
)
})
set_linetype <- setNames(
rep("solid", length(timeStepDurations)),
unique(out$approach)
)
# --- Manually set color palettes -----
set_linetype <- setNames(
rep("solid", length(errorTolerances)),
unique(out$approach)
)
set_color <- setNames(
colorspace::qualitative_hcl(length(errorTolerances)),
unique(out$approach)
)
# --- Merge with deSolve for plotting -----
bind_rows(
rescaled_desolve %>%
select(time, S, E, I, R) %>%
rename(Time = time) %>%
mutate(
approach = "deSolve (baseline)"
),
out
) %>%
pivot_longer(cols = c("S", "E", "I", "R"), values_to = "prop", names_to = "comp") %>%
# ----- Plot ------
ggplot() +
geom_line(
aes(x = Time, y = prop, color = approach, linetype = approach)
) +
scale_color_manual(
values = c("deSolve (baseline)" = "black", set_color)
) +
scale_linetype_manual(
values = c("deSolve (baseline)" = "dashed", set_linetype)
) +
theme_bw() +
theme(legend.title = element_blank()) +
facet_wrap(~ comp, scales = "free") +
labs(
title = "Denim output at different timeStep",
x = "Time",
y = "Prop"
)
timeStep <- 0.01
errorTolerance <- 0.001
denim_model <- denim_dsl({
S -> E = (R0/tr) * S * (I/N)
E -> I = d_gamma(rate = 1/4, shape = 2)
I -> R = d_gamma(rate = 1/3, shape = 2)
})
initialValues <- c(S = 999999, E = 1, I= 0, R= 0)
parameters <- c(R0 = 3.5,
tr = 3*2, # compute mean recovery time, for gamma it's scale*shape
N = 1e6)
errorTolerances <- c(0.001, 0.01, 0.1, 0.2)
# --- generate output at each time step -----
out <- map_dfr(errorTolerances, \(x){
data <- sim(
transitions = denim_model,
initialValues = initialValues,
parameters = parameters,
simulationDuration = sim_duration,
timeStep = 0.01,
errorTolerance = x
) %>%
mutate(
S = S/1e6, E = E/1e6, I = I/1e6, R = R/1e6
)
data %>%
filter(Time %% 1 == 0) %>%
mutate(
approach = paste0("denim errorTolerance=", x)
)
})
# --- Manually set color palettes -----
set_linetype <- setNames(
rep("solid", length(errorTolerances)),
unique(out$approach)
)
set_color <- setNames(
colorspace::qualitative_hcl(length(errorTolerances)),
unique(out$approach)
)
# --- Merge with deSolve for plotting -----
bind_rows(
rescaled_desolve %>%
select(time, S, E, I, R) %>%
rename(Time = time) %>%
mutate(
approach = "deSolve (baseline)"
),
out
) %>%
pivot_longer(cols = c("S", "E", "I", "R"), values_to = "prop", names_to = "comp") %>%
# ----- Plot ------
ggplot() +
geom_line(
aes(x = Time, y = prop, color = approach, linetype = approach)
) +
scale_color_manual(
values = c("deSolve (baseline)" = "black", set_color)
) +
scale_linetype_manual(
values = c("deSolve (baseline)" = "dashed", set_linetype)
) +
theme_bw() +
theme(legend.title = element_blank()) +
facet_wrap(~ comp, scales = "free") +
labs(
title = "Denim output at different errorTolerance",
x = "Time",
y = "Prop"
)
library(tidyverse)
# loading cached run time if available
cached_runtime <- if (file.exists("../supplements/cached_runtime.rds")) {
readRDS("../supplements/cached_runtime.rds")
} else {
NULL
}
# compute min, max, variance
# lapply(cached_runtime, function(x) c(min = min(x), max = max(x), var = var(x)))
total_runs <- 50L # number of runs
sim_duration <- 180 # duration of simulation
library(reticulate)
# use_python("/opt/anaconda3/envs/bnn/bin/python", required = TRUE)
use_condaenv(condaenv='bnn', required = TRUE)
# matplotlib <- import("matplotlib")
# matplotlib$use("Agg", force = TRUE)
py_run_file("../supplements/useir_python.py")
# py_config()
reticulate::repl_python()
python_runs <- if(is.null(cached_runtime)){
py$python_runs
}else{
cached_runtime$python_runs
}
python_runs
reticulate::repl_python()
